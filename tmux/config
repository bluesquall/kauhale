# configuration for tmux
# (see footer for resources)

# reload (this) config file
bind r source-file "$XDG_CONFIG_HOME"/tmux/config

# improve colors
set -g default-terminal "screen-256color"

# set up status bar
set -g status-bg 'yellow'
set -g status-fg 'red'

# vi status and mode keys
set -g status-keys vi
setw -g mode-keys vi

# vim style pane selection
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R


# resources:
# - https://github.com/thoughtbot/dotfiles
# - https://robots.thoughtbot.com/love-hate-tmux
# - http://www.hamvocke.com/blog/a-guide-to-customizing-your-tmux-conf/
# - https://github.com/gpakosz/.tmux

bind-key L pipe-pane -o "exec cat >> $HOME/'tmux.%Y-%m-%d.#{session_name}-#{window_index}-#{pane_index}.log'" \; display-message 'Loggled'
# see also: https://github.com/tmux-plugins/tmux-logging
